[project]
name = "gapless-crypto-data"
version = "3.3.0"
description = "Ultra-fast cryptocurrency data collection with zero gaps guarantee. 22x faster via Binance public repository with complete 13-timeframe support (1s-1d) and intelligent monthly-to-daily fallback. Provides 11-column microstructure format with order flow metrics."
readme = { file = "README.md", content-type = "text/markdown" }
authors = [
    { name = "Eon Labs", email = "terry@eonlabs.com" }
]
maintainers = [
    { name = "Terry Li", email = "terry@eonlabs.com" }
]
license = { text = "MIT" }
keywords = [
    "cryptocurrency", "crypto", "binance", "data", "collection",
    "trading", "OHLCV", "gap-filling", "financial-data", "time-series",
    "microstructure", "order-flow", "liquidity", "taker-volume", "authentic-data",
    "api", "pandas", "download", "fetch-data", "function-based", "ccxt",
    "timeframe", "interval", "dual-parameter", "backward-compatibility",
    "ultra-high-frequency", "1s-1d", "13-timeframes", "monthly-daily-fallback",
    "zero-gaps", "22x-faster", "performance", "gapless"
]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "Intended Audience :: Financial and Insurance Industry",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Office/Business :: Financial :: Investment",
    "Topic :: Scientific/Engineering :: Information Analysis",
    "Topic :: Software Development :: Libraries :: Python Modules",
]
requires-python = ">=3.9"
dependencies = [
    "duckdb>=1.1.0",  # Single-file OLAP database for validation report persistence
    "httpx>=0.25.0",
    "pandas>=2.0.0",
    "pydantic>=2.0.0",  # Data validation and OpenAPI schema generation for validation reports
    "pyarrow>=16.0.0",  # Required by pandas for Parquet I/O (df.to_parquet/read_parquet engine)
]

[project.urls]
Homepage = "https://github.com/terrylica/gapless-crypto-data"
Documentation = "https://github.com/terrylica/gapless-crypto-data#readme"
Repository = "https://github.com/terrylica/gapless-crypto-data.git"
Issues = "https://github.com/terrylica/gapless-crypto-data/issues"
Changelog = "https://github.com/terrylica/gapless-crypto-data/blob/main/CHANGELOG.md"

[project.scripts]
# DEPRECATED: CLI will be removed in v4.0.0 - Use Python API instead
gapless-crypto-data = "gapless_crypto_data.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/gapless_crypto_data"]

[tool.hatch.build.targets.wheel.force-include]
"src/gapless_crypto_data/py.typed" = "gapless_crypto_data/py.typed"

[tool.ruff]
target-version = "py39"
line-length = 100

[tool.ruff.lint]
select = [
    "E",  # pycodestyle errors
    "W",  # pycodestyle warnings
    "F",  # pyflakes
    "I",  # isort
    "C",  # flake8-comprehensions
    "B",  # flake8-bugbear
]
ignore = [
    "E501",  # line too long, handled by black
    "C901",  # function too complex (legacy code)
    "F841",  # unused variables (test files)
    "W291",  # trailing whitespace
    "W293",  # blank line contains whitespace
    "B904",  # exception chaining warnings (non-critical for publishing)
    "B007",  # unused loop variables (non-critical for publishing)
]

[tool.ruff.lint.isort]
known-first-party = ["gapless_crypto_data"]

[tool.black]
target-version = ['py39']
line-length = 100

[tool.mypy]
python_version = "3.9"
warn_return_any = true
warn_unused_configs = true
# Gradually improve type checking
disallow_untyped_defs = false
check_untyped_defs = true
warn_redundant_casts = true
warn_unused_ignores = true
no_implicit_optional = true
strict_optional = true

# Ignore missing imports for now
ignore_missing_imports = false

# Per-module configuration for gradual adoption
[[tool.mypy.overrides]]
module = [
    "gapless_crypto_data.cli",
    "gapless_crypto_data.__init__"
]
disallow_untyped_defs = true

# Enforce strict mode on SDK entry points (v3.2.0)
[[tool.mypy.overrides]]
module = "gapless_crypto_data.api"
disallow_untyped_defs = true

[[tool.mypy.overrides]]
module = "gapless_crypto_data.__probe__"
disallow_untyped_defs = true

[[tool.mypy.overrides]]
module = "tests.*"
ignore_errors = true

[tool.pytest.ini_options]
minversion = "7.0"
addopts = "-ra -q --strict-markers"
testpaths = [
    "tests",
]
markers = [
    "integration: marks tests as integration tests (may require network access)",
    "asyncio: marks tests as async tests using asyncio",
]

[dependency-groups]
dev = [
    "mypy>=1.18.1",
    "pandas-stubs>=2.2.2.240807",
    "pre-commit>=4.3.0",
    "pytest>=8.4.2",
    "pytest-cov>=7.0.0",
    "pytest-asyncio>=0.21.0",
    "ruff>=0.13.0",
    "types-requests>=2.32.4.20250913",
    "yamllint>=1.37.1",
    "commitizen>=4.9.1",
]

[tool.commitizen]
name = "cz_conventional_commits"
tag_format = "v$version"
version_scheme = "semver"
version_provider = "pep621"
major_version_zero = false
bump_message = "chore(release): bump version $current_version → $new_version [skip ci]"
